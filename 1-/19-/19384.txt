int * pkati; // pointer auf Ida
pkati = (int *) malloc(sizeof(kati)); // Dem pointer zeigen, wo
                                      // er Ida findet

in pkati steht jetzt eine Speicheradresse drin, also eine
Referenz. Wenn du jetzt an Ida rankommen willst, mußt du die
Referenz dereferenzieren.

mfg, 
	Werner
-- 
BOFH excuse #419:
Repeated reboots of the system failed to solve problem